#pragma once

class Particle {
	//contains the general properties of a type of particle
private:
	double particle_mass;
	double particle_radius;
public:
	Particle(double mass, double radius);
	//not sure what methods it will have
};